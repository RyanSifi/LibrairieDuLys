<!DOCTYPE html>
<html>
    <head>
        <!-- Google tag (gtag.js) -->
        <script async src="https://www.googletagmanager.com/gtag/js?id=G-KZS6VRMVK2"></script>
        <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());

        gtag('config', 'G-KZS6VRMVK2');
        </script>
        <meta charset="UTF-8">
        <title>{% block title %}Librairie du Lys{% endblock %}</title>
        <meta name="description" content="Découvrez notre sélection de livres à la Librairie du Lys, votre référence pour les meilleurs romans, Contes et bien plus encore.">
        <link rel="icon" type="image/x-icon" href="{{ asset('images/icon_librairie.png') }}" />
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/2.9.3/umd/popper.min.js"></script>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/bootstrap/css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('assets/bootstrap/css/bootstrap-icons.css') }}">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}

        {% block javascripts %}
        <script src="https://code.jquery.com/jquery-3.6.0.slim.min.js"></script>
        <script src="{{ asset('assets/bootstrap/js/bootstrap.bundle.min.js') }}"></script>
        <script src="{{ asset('assets/bootstrap/js/bootstrap.min.js') }}"></script>
        {% endblock %}
    </head>
    <body>
            {% block navbar %}
                <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="{{ path('app_homepage') }}">Librairie Du Lys
                    <img src="{{ asset('images/logo_librairie_seul.png') }}" width="60" height="60" alt="Logo de la Librairie du Lys">
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_romans') }}">Roman</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_fantasy') }}">Fantasy</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_contes') }}">Conte et Fable</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_theatre') }}">Théâtre</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_poesie') }}">Poésie</a>
                    </li>
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_genre_autobiographie') }}">Autobiographie</a>
                    </li>
                    {% if not app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                    {% endif %}
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">Profile</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se deconnecter</a>
                    </li>
                    {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('admin_page') }}">ADMIN</a>
                        {% endif %}
                    </ul>
                </div>
            </nav>
        {% endblock %}
        {% block body %}
            <div class="hero">
                <h1 class="title">Bienvenue à la Librairie du Lys</h1>
                <p>Découvrez notre sélection de livres</p>
            </div>
        {% endblock %}
        {% block footer %}
            <footer class="bg-dark text-center text-white">
                <div class="container p-4">
                    <div class="row">
                    <div class="col-md-4">
                        <h5>A propos</h5>
                        <p>La Librairie Du Lys est une librairie qui aspire à garder le contact humain au sein des passionné de lecture.</p>
                    </div>
                    <div class="col-md-4">
                        <h5>Où nous trouver</h5>
                        <p>Adresse postale<br>
                        60, rue du lys<br>
                        77190 Dammarie-les-lys.</p>
                    </div>
                    <div class="col-md-4">
                        <h5>Contactez-nous</h5>
                        <br>
                        <section class="mb-4">
                        <a href="mailto:ryansifi@librairiedulys.fr" class="text-white text-decoration-none link">Envoyer nous un e-mail</a>
                        </section>
                    </div>
                    </div>
                    <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2);">
                        © 2023 Copyright:
                        <a class="text-white" href="http://librairiedulys.fr/">librairiedulys.fr</a>
                        <a class="text-white" href="{{ path('app_mentions_legales') }}">Mentions Légales</a>
                        <a class="text-white" href="{{ path('app_politiques_confidentialite') }}">Politiques de Confidentialité</a>
                    </div>
            </footer>
            {% endblock %}
    </body>
</html>
