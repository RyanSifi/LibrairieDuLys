<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('assets/bootstrap/css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('assets/bootstrap/css/bootstrap-icons.css') }}">
            <style>
        /* Définition des couleurs */
        :root {
            --primary-color: #1d3557; /* Bleu foncé */
            --secondary-color: #6b705c; /* Vert olive */
            --accent-color: #8d2d2c; /* Bordeaux */
        }

        /* Styles spécifiques à la page d'accueil */
        .hero {
            background-color: var(--primary-color);
            color: #fff;
            padding: 100px;
            text-align: center;
        }

        .latest-books {
            background-color: var(--secondary-color);
            color: #fff;
            padding: 50px;
        }

        .latest-books h2 {
            color: var(--accent-color);
        }

        .book-list {
            list-style: none;
            padding: 0;
        }

        .book-list li {
            margin: 20px 0;
        }

        .book-title {
            font-size: 24px;
            font-weight: bold;
        }

        .book-author {
            font-size: 16px;
            font-style: italic;
        }
        .book-description {
            font-size: 16px;
            line-height: 1.4;
            margin-top: 10px;
        }
        .book-cover {
            width: 200px;
            height: 300px;
            margin-left: auto;
        }

        .title {
            display: inline-block;
            margin-right: 20px;
            }

        .logo {
            display: inline-block;
            float: right;
            width: 200px;
            height: 200px;
            margin-top: -50px;
        }

        /* Styles spécifiques à la page de recherche */
        .search-form {
            margin: 50px auto;
            max-width: 600px;
        }

        .search-results {
            margin: 50px auto;
            max-width: 800px;
        }

        .search-results .book-list li {
            margin: 10px 0;
        }

        .search-results .book-title {
            font-size: 20px;
        }

        .search-results .book-author {
            font-size: 14px;
        }
    </style>
        {% endblock %}

        {% block javascripts %}
        <script src="{{ asset('assets/bootstrap/js/bootstrap.min.js') }}"></script>
        {% endblock %}
    </head>
    <body>
    {% block navbar %}
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">My Site</a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item active">
                            <a class="nav-link" href="{{ path('app_homepage') }}">Page d'accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_profile') }}">Profile</a>
                        </li>
                    </ul>
                </div>
            </nav>
        {% endblock %}
        {% block body %}
            <div class="hero">
                <img src="{{ asset('images/save_logo.png') }}" alt="Logo Librairie du Lys" class="logo">
                <h1 class="title">Bienvenue à la Librairie du Lys</h1>
                <p>Découvrez notre sélection de livres</p>
            </div>
        {% endblock %}
    </body>
</html>
